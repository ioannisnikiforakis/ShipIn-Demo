#!/bin/bash

# TODO make a simple crontab installer that will add a line to the root user's crontab
# This will ensure execution of the data collection script every X minutes.
# Get X as an option from the user. Default value could be as low as 5 mins?
